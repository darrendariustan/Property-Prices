services:
  - type: web
    name: apartment-price-estimator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run apartment_price_estimator.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
    plan: free 